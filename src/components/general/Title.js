const Title = ( {title} ) => {
    
    return (
        <h2 className="section-title h2"><span className="highlight-container"><span className="highlight"> {title} </span></span></h2>
)};

export default Title;